// forEach= es una operación que se realiza para recorrer un arreglo. 

let herramientas=["martillo", "alicate", "serrucho", "tijeras"]; 

herramientas.forEach(element => {
    console.log (element); 
});

let colores=["rojo", "negro", "naranja", "azul", "verde"]; 

colores.forEach(element => {
    console.log(colores[3]);
});